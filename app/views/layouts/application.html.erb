<!DOCTYPE html>
<html>
  <head>
    <title>TestTask</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="text-center"  data-turbolinks="false">

    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
      <header class="masthead mb-auto">
        <div class="inner">
          <h3 class="masthead-brand">My test-task</h3>
          <nav class="nav nav-masthead justify-content-center">

            <% if user_signed_in? %>
				<%= link_to "Logout", destroy_user_session_path, :method => :delete , class:"nav-link"%>
			<% else %>
				<%=link_to 'Register', new_user_registration_path, class:"nav-link active" %>
				<%=link_to 'Login', new_user_session_path, class:"nav-link " %>
			<% end %>
          </nav>
        </div>
      </header>

      <main role="main" class="inner cover">
        	<p class="lead notice"><%= notice %></p>
		<p class="lead alert"><%= alert %></p>
        <%= yield %>
      </main>

      <footer class="mastfoot mt-auto">
        <div class="inner">
          <p>Cover template for <a href="https://getbootstrap.com/">Bootstrap</a>, by <a href="https://twitter.com/mdo">@mdo</a>.</p>
        </div>
      </footer>
    </div>

  </body>
</html>
