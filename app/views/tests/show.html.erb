<h1>Its test <%= @test.title %>!</h1>
<p>Lets start test!</p>
      <ul>
       <% @test.questions.each do |question| %>
         <li><%= question.title %> </li>
       <% end %>
      </ul>
<%= form_for [@test, @question], url: question_ask_path(@test, @question.id), method: "get" do |f| %>
<%= f.submit "Late" %>
<% end %>
